import Flow from '@/components/Flow';

export const metadata = {
  title: 'Flow Converter | Calculator',
  description: 'Convert volumetric flow rates between m³/s, L/s, L/min, ft³/s, gal/min, and more.',
};

export default function Page() {
  return <Flow />;
}
